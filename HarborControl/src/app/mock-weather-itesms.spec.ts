import { MockWeatherItesms } from './mock-weather-itesms';

describe('MockWeatherItesms', () => {
  it('should create an instance', () => {
    expect(new MockWeatherItesms()).toBeTruthy();
  });
});
